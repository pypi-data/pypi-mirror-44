_vss_completion() {
    COMPREPLY=( $( env COMP_WORDS="${COMP_WORDS[*]}" \
                   COMP_CWORD=$COMP_CWORD \
                   _VSS_COMPLETE=complete $1 ) )
    return 0
}

complete -F _vss_completion -o default vss;
