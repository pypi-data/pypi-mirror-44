from setuptools import setup

setup(name='ri_distributions',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['ri_distributions'],
      autor = 'Ralph I',
      zip_safe=False)
