.. highlight:: shell

============
Installation
============


Stable release
--------------

To install Checking SMTP STARTTLS, run this command in your terminal:

.. code-block:: console

    $ pip install checking_smtp_starttls

This is the preferred method to install Checking SMTP STARTTLS, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for Checking SMTP STARTTLS can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/dibisis/checking_smtp_starttls

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/dibisis/checking_smtp_starttls/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/dibisis/checking_smtp_starttls
.. _tarball: https://github.com/dibisis/checking_smtp_starttls/tarball/master
