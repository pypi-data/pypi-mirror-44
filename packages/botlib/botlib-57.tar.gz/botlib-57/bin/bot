#!/usr/bin/python3

import os, sys ; sys.path.insert(0, os.getcwd())

import readline

from bot import __version__
from obj.shell import Shell, start, startup, reset

startup()

try:
    start("bot", version=__version__)
except (KeyboardInterrupt, EOFError):
    print("")

reset()
os._exit(0)
