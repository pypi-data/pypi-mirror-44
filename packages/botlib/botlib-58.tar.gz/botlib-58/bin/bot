#!/usr/bin/python3

import os, sys ; sys.path.insert(0, os.getcwd())

from bot import cfg, __version__
from bot.run import kernel
from obj.shell import reset, startup

startup()
try:
    kernel.start("botlib", version=__version__)
except KeyboardInterrupt:
    print("")
reset()
os._exit(0)
